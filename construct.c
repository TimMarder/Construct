#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main(){
  struct account first;
  first.name = "hello";
  first.bal = 5;
  int myacc();
}

makeacc(){



}

int myacc(){
  printf("%s %d\n", first.name,first.bal);
  return 0;
}

int changebal(
